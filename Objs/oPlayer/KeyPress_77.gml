if (audio_is_paused(mLofi))
{
	audio_resume_sound(mLofi);
	}
	
	else {
		audio_pause_sound(mLofi);} 
		
		
		if (audio_is_paused(Attack_season4))
{
	audio_resume_sound(Attack_season4);
	}
	
	else {
		audio_pause_sound(Attack_season4);} 
		
		if (audio_is_paused(mAot))
{
	audio_resume_sound(mAot);
	}
	
	else {
		audio_pause_sound(mAot);} 